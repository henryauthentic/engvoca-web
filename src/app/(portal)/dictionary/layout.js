export const metadata = {
  title: "Từ điển | ENG VOCA",
  description: "Tra cứu từ vựng tiếng Anh với phiên âm, nghĩa, ví dụ, từ đồng nghĩa và trái nghĩa. Hỗ trợ API từ điển thật.",
};

export default function DictionaryLayout({ children }) {
  return children;
}
