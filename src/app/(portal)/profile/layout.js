export const metadata = {
  title: "Hồ sơ cá nhân | ENG VOCA",
  description: "Xem tiến trình học tập, huy hiệu, lịch sử hoạt động và từ vựng đã lưu.",
};

export default function ProfileLayout({ children }) {
  return children;
}
